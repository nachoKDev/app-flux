apiVersion: batch/v1
kind: Job
metadata:
  name: migrationjob
  namespace: platform-dev
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrationjob
          image: public.ecr.aws/docker/library/busybox:unstable-musl
          command:
            - sh
            - -c
            - echo "Running migrations"
